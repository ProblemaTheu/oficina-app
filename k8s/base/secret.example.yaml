# TEMPLATE — não aplicar este arquivo com valores reais versionados!
#
# Opção 1 (recomendada): criar o secret direto do seu .env:
#   ./scripts/k8s-local-deploy.sh  (faz isso automaticamente)
#
# Opção 2: copiar para secret.yaml (gitignorado), preencher e aplicar:
#   cp k8s/base/secret.example.yaml k8s/base/secret.yaml
#   kubectl apply -f k8s/base/secret.yaml
#
# Evolução futura: selar com Sealed Secrets ou SOPS para versionar cifrado.
apiVersion: v1
kind: Secret
metadata:
  name: oficina-secrets
  namespace: oficina
  labels:
    app.kubernetes.io/part-of: tech-challenge
type: Opaque
stringData:
  DB_USER: postgres
  DB_PASSWORD: troque-me
  JWT_SECRET: troque-me
  WEBHOOK_SECRET: troque-me
  # Opcionais — só para provedor SMTP real com autenticação:
  # SMTP_USER: ""
  # SMTP_PASSWORD: ""
